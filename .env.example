# Optional: Set to your live WebSocket backend URL (e.g. on Render, Railway, etc.)
# If left unset, the frontend connects to http://<current-domain>:3001
VITE_SOCKET_URL=https://your-mpl-server.onrender.com
